{#-
  Helpers for the operations SMT-LIB lacks for `Real`. Defined once rather
  than inlined because each is an `ite` that would otherwise duplicate its
  argument two or three times, and nesting a few of those turns a small
  expression into an enormous one. `emit::tests::the_prelude_helpers_mean_what_they_say`
  puts the meaning of every one of these to Z3; swap `expr_min`'s `<=` for
  `>=` and nothing else in the crate would notice.

  Babel's `sgn` follows Java: zero maps to zero. Rust's `f64::signum` gives
  1.0 there, so this encodes babel's version and not the host's.

  `to_int` is floor and not truncation, negatives included: `to_int` of -2.7
  is -3. That is what lets `expr_ceil` work by reflection, and it is why `%`
  cannot be written over `expr_floor`.

  `expr_trunc` is not reachable from babel source; it exists because `%`
  needs it. Babel's `%` is a *remainder* and not a modulo, and the difference
  is the sign: a remainder takes the dividend's, so `-7 % 3` is -1, where a
  modulo takes the divisor's and answers 2. Java's `%` and Rust's agree on the
  remainder, which is why `BinaryOp::Rem::apply` can be a bare `a % b`.

  No trailing newline: the document template supplies the line breaks.
-#}
(define-fun expr_abs ((x Real)) Real (ite (< x 0.0) (- x) x))
(define-fun expr_sqr ((x Real)) Real (* x x))
(define-fun expr_cube ((x Real)) Real (* x x x))
(define-fun expr_max ((a Real) (b Real)) Real (ite (>= a b) a b))
(define-fun expr_min ((a Real) (b Real)) Real (ite (<= a b) a b))
(define-fun expr_sgn ((x Real)) Real (ite (< x 0.0) (- 1.0) (ite (> x 0.0) 1.0 0.0)))
(define-fun expr_floor ((x Real)) Real (to_real (to_int x)))
(define-fun expr_ceil ((x Real)) Real (- (to_real (to_int (- x)))))
(define-fun expr_trunc ((x Real)) Real (ite (>= x 0.0) (expr_floor x) (expr_ceil x)))
(define-fun expr_rem ((a Real) (b Real)) Real (- a (* b (expr_trunc (/ a b)))))
{#- -#}
